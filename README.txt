# 目標
1. 指定フォルダにある画像データをGCSへアップロードする
2. アップロードが終了したファイルを、ファイルの命名規則に従って指定ディレクトリへ移動する。ディレクトリがなければ作成する。

# 前提
1. 指定フォルダ内にある画像の命名規則は、"{`\d{15}_[123456].jpg" である。
2. 各ファイルの先頭15文字の数字を IMEI と呼ぶ。
3. IMEI 毎にディレクトリは作成され、同一IMEIを持つファイルがそこに格納される。

# 作業メモ
1. gcloud init
2. gcloud auth